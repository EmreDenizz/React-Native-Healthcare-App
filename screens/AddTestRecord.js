import * as React from 'react';
import { Text, View, Button, StyleSheet, TouchableOpacity } from 'react-native';
import { useState } from 'react'

export default function AddTestRecord({route}) {
    return (
        <View> 
            <Text> 
                Add test record
            </Text> 
        </View> 
    );
}
