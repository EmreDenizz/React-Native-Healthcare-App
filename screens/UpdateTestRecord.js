import * as React from 'react';
import { Text, View, Button, StyleSheet, TouchableOpacity } from 'react-native';
import { useState } from 'react'

export default function UpdateTestRecord({route}) {
    return (
        <View> 
            <Text> 
                Update test record
            </Text> 
        </View> 
    );
}
