import {StyleSheet} from 'react-native';

// Style definitions
const styles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
    },
    wrapper: {
        padding: 30,
    }
});

module.exports = styles;
